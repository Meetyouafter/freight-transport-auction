# widgets

Composite, self-contained UI blocks assembled from `entities` and `features`
(e.g. a page header, a lot card with an embedded bid button). A widget may not
know about a specific page — it should be reusable across routes.
